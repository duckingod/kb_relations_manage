/*
var sqlite3 = require('sqlite3').verbose();
var db = new sqlite3.Database('test.db');

db.serialize(function() {
    db.run("CREATE TABLE kbtriples (id int, first TEXT, relation TEXT, second TEXT, symmetric BOOLEAN)");
});

db.close();
*/
